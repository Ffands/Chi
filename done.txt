Привет! Я нашёл и исправил две ошибки, которые не давали проекту скомпилироваться:
1. Забыл добавить импорт `android.widget.HorizontalScrollView`, из-за чего ругался класс `UIManager`.
2. Переменные кнопок (`linesToggleBtn`, `hotbarToggleBtn` и т.д.) использовались в функции скрытия меню (внутри `minMaxBtn`) до того, как они были объявлены. Я перенёс объявление `minMaxBtn` в правильное место (ниже всех остальных кнопок).

Всё поправлено. Запускайте билд в GitHub Actions — теперь должно собраться без проблем! Жду результат!
