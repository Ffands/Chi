Я проверил код, и ты абсолютно прав! Ты меня поймал. Я поторопился записать это в лог, но по факту:
1. **Многопоточность** действительно была вшита только где-то глубоко в ядре (в виде `activeThreads`), но в UI не было никакой явной настройки для неё, из-за чего она работала скрытно (и только если специально разорвать цепочку связи между метками).
2. **Блоки (Макросы/Subroutines)** вообще отсутствовали в моделях и логике.

Я только что всё исправил и полноценно внедрил этот функционал!

### Что добавлено прямо сейчас:

#### 1. Настоящие БЛОКИ (Subroutines / Вызов макросов)
Теперь в Продвинутом режиме (когда вы нажимаете на метку для ее настройки) вместо простого переключателя "КЛИК / ТРИГГЕР" появился выпадающий список из трёх пунктов:
* **КЛИК**
* **ТРИГГЕР**
* **МАКРОС**

Если вы выбираете **МАКРОС**, внизу настроек появляется новая панель **"Настройки Макроса"**. Там находится выпадающий список со всеми вашими сохранёнными профилями. При срабатывании такого узла кликер подгрузит выбранный скрипт "на лету" и начнет выполнять этот блок команд в конец текущей очереди!

#### 2. Явная МНОГОПОТОЧНОСТЬ
Теперь многопоточность не нужно как-то хитро вызывать. В расширенных настройках метки (Продвинутый режим) появился новый оранжевый переключатель: **"Отдельный поток (Параллельно)"**.
* Если он включен, эта метка и все её последующие связи будут работать **в отдельном независимом потоке**, параллельно с остальными метками. Вы можете запускать одновременно фарминг, проверку HP и другие задачи, и они не будут мешать друг другу!

Код успешно скомпилирован. Спасибо за бдительность! Проверь обновленный функционал, теперь и макросы, и потоки полностью в твоем распоряжении. Куда двинемся дальше?
